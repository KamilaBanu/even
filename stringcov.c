#include<stdio.h>
#include<stdlib.h>
int main() {
   int n;
   char marks[3];
   printf("Please Enter Marks : ");
   scanf("%s", marks);
   num = atoi(marks);
   printf("Marks : %d", n);
   return (0);
}
